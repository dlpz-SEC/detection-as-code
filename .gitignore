# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
venv/
.venv/
ENV/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Pipeline outputs (regenerated on each run)
output/
*.json
# Keep test samples
!tests/samples/**/*.json
# Keep config files
!configs/*.json
# Keep coverage outputs and rule manifest
!docs/*.json

# Local testing artifacts
validation-report.json
test-results.json
coverage-report.json

# Logs
*.log

# Secrets (never commit these)
.env
*.pem
*.key
secrets/

# Local session notes — these record live lab state (manager IP, credential
# status, which rules are deployed). This repo is public; keep them local.
Handoff.md
LESSONS.md
